[pytest]
asyncio_mode = auto
# One event loop for the whole session: asyncpg connections cannot migrate loops.
asyncio_default_fixture_loop_scope = session
asyncio_default_test_loop_scope = session
testpaths = tests
